name: Build Python EXE

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install PyInstaller
        run: |
          pip install pyinstaller pywin32-ctypes
      
      - name: Build EXE
        run: |
          pyinstaller --onefile --windowed --name DiskCleaner --hidden-import=ctypes.wintype --hidden-import=ctypes disk_cleaner.py
      
      - name: Check EXE
        shell: pwsh
        run: |
          Get-ChildItem dist/DiskCleaner.exe | Format-Table Name, Length -AutoSize
      
      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: DiskCleaner-EXE
          path: dist/DiskCleaner.exe
          retention-days: 30
